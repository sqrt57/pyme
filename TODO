- Compiler and evaluator:
    + constants
    + variable read
    + procedure call
    + if
    + quote
    + lambda
    + define
    + set!
    + define procedure
    - eval
    - apply
+ Disassembler
+ Full interpreter
- Benchmarks
- Custom types
- Custom reader
- Custom compiler
- Make all runtime errors subclass EvalError or SchemeError
