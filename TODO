- Compiler and evaluator:
    + constants
    + variable read
    + procedure call
    + if
    + quote
    + lambda
    + define
    + set!
    - eval
- Full interpreter
- Tests in Scheme
- Custom types
- Custom reader
- Custom compiler
- Disassembler
- Benchmarks
- Make all runtime errors subclass EvalError or SchemeError
